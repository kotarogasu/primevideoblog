.video-content
  .video-tweet
    .video-image{:style => "background-image: url(#{@tweet.image});"}
    .more
      %a{:href => "/users/#{@tweet.user.id}"}
        %p.tweet-name
          投稿者：#{@tweet.user.nickname}
      %p.tweet-date= (@tweet.updated_at.strftime("%Y/%m/%d %H:%M"))
      %p.video-title= (@tweet.title)
      %p.video-text= (@tweet.text)
      .video-under
        - if user_signed_in? && current_user.id == @tweet.user_id
          = link_to  'edit', "/tweets/#{@tweet.id}/edit", method: :get, class:"video-btn__edit"
          = link_to  'delete', "/tweets/#{@tweet.id}", method: :delete, class:"video-btn__delete"
        = link_to  'Primevideoへ', url=@tweet.link, class:"video-btn__link"
